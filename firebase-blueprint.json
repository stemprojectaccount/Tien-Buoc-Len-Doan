{
  "entities": {
    "User": {
      "title": "User",
      "description": "Student user profile",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "name": { "type": "string" },
        "birthDate": { "type": "string", "format": "date" },
        "class": { "type": "string", "enum": ["9A", "9B", "9C", "9D"] }
      },
      "required": ["uid", "name", "birthDate", "class"]
    },
    "QuizResult": {
      "title": "QuizResult",
      "description": "Student quiz submission",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "studentName": { "type": "string" },
        "studentBirthDate": { "type": "string", "format": "date" },
        "studentClass": { "type": "string" },
        "answers": { "type": "array" },
        "score": { "type": "number" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "studentName", "studentBirthDate", "studentClass", "answers", "score", "timestamp"]
    }
  },
  "firestore": {
    "/users/{uid}": {
      "schema": "User",
      "description": "User profiles"
    },
    "/results/{resultId}": {
      "schema": "QuizResult",
      "description": "Quiz results"
    }
  }
}
